<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OptionButtonTheme" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:tint">@color/option_button_selector</item>
    </style>

    <style name="SaveButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@drawable/oval_button</item>
        <item name="android:backgroundTint">@color/save_button_color_selector</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="PlayButton" parent="Base.Widget.AppCompat.Button.Borderless">
        <item name="android:src">@color/play_button_selector</item>
        <item name="android:background">?android:selectableItemBackgroundBorderless</item>
    </style>

    <style name="RemoveButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:background">@drawable/oval_button</item>
        <item name="android:backgroundTint">@color/colorRed</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
